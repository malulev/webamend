[Unit]
Description=Collect Webamend metrics every minute

[Timer]
OnBootSec=2min
OnUnitActiveSec=60s
# Without this every host provisioned from the same image collects on the same
# second.
RandomizedDelaySec=10s
Unit=webamend-probe.service

[Install]
WantedBy=timers.target
