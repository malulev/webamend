# One installation, one website (constitution VI). This file starts that one
# installation: the application, which is both the editing interface and the
# worker, and nothing else. There is no database service here, and adding one
# is a decision to be argued for in writing (constitution VII).
#
# The agent image is built separately and is deliberately not a service — it
# is not a long-running process but a throwaway container started per request
# and destroyed afterwards:
#
#   docker build -t webagent/agent:latest agent/
#
# ---------------------------------------------------------------------------
# THE DOCKER SOCKET IS ROOT ON THE HOST. READ THIS BEFORE RUNNING IT.
# ---------------------------------------------------------------------------
#
# `/var/run/docker.sock` is mounted below because the application starts the
# agent container for each request. Access to that socket is equivalent to
# root on the host machine: anything that can talk to it can start a container
# with the host filesystem mounted and as much privilege as it likes. Any
# remote code execution in this application is therefore host compromise, not
# container compromise.
#
# What this installation already does about it, in the application itself:
#   - the agent container receives no GitHub token, no Netlify token and no
#     git remote (contracts/repo-files.md), so the code that runs untrusted
#     model output holds no credential and cannot push anywhere;
#   - it runs with every capability dropped and `no-new-privileges`;
#   - it is destroyed after every request, successful or not.
#
# That protects the repository from the agent. It does not protect the host
# from this application. The hardening path, in the order worth taking it:
#
#   1. Put a socket proxy in front of the daemon (for example
#      tecnativa/docker-socket-proxy) and give this service that proxy's
#      address as DOCKER_HOST instead of the raw socket. Allow only the calls
#      the runner actually makes — create, start, attach, wait, remove — and
#      deny the rest of the API. This is the cheapest large improvement.
#   2. Run the daemon rootless, so the socket's authority is a single
#      unprivileged user's rather than root's.
#   3. Run the agent workload on a separate host or VM from the interface, and
#      give this service no path to a production daemon at all.
#
# None of these are configured here, because a default that quietly half-works
# is worse than a default that states plainly what it is.
#
# `ops/` takes step 2 for a multi-client host: one Linux user per client, each
# running its own rootless dockerd, and DOCKER_SOCK below pointed at that
# user's socket. The socket is still the authority of whoever owns it — that is
# now one unprivileged client user rather than root, and one client's
# compromise reaches neither the host nor another client's state directory.
# Nothing about that is automatic on a plain `docker compose up`; see
# ops/README.md.

services:
  app:
    # Two ways in, and which one applies is decided by whether APP_IMAGE names
    # a tag that already exists. On a development machine nothing sets it, the
    # tag `lexi/app:local` is not in the daemon, and Compose falls back to
    # the `build:` block below — one file, one `docker compose up`, no registry.
    # On a client host `ops/release.sh` sets APP_IMAGE to a tag it has already
    # built once and pushed to the host registry, so twenty installations pull
    # the same layers instead of each running `npm ci && npm run build` on a
    # 2 vCPU box. Keeping both here is what lets the same file serve both.
    image: ${APP_IMAGE:-lexi/app:local}

    # Built inline rather than from a Dockerfile of its own: the whole
    # deployment is meant to be one file to read, and the application image is
    # eight lines of it.
    build:
      context: .
      # Requires Docker Compose v2.17 or newer.
      dockerfile_inline: |
        # Two stages, for one reason: every client on a shared host runs its
        # own rootless daemon and therefore its own image store, so the image
        # size is paid per client rather than once. A single-stage build ships
        # the toolchain, the dev dependencies and the build cache — about
        # 2 GB, which is 40 GB across twenty clients. The standalone output is
        # roughly a tenth of that.
        FROM node:22-slim AS build
        WORKDIR /app
        COPY package.json package-lock.json ./
        RUN npm ci
        COPY . .
        RUN npm run build

        FROM node:22-slim
        # git is the application's own tool, not the agent's: the host clones
        # the mirror, checks out the working tree, and makes every commit.
        # The agent image deliberately has no git at all.
        RUN apt-get update && apt-get install -y --no-install-recommends git ca-certificates \
            && rm -rf /var/lib/apt/lists/*
        WORKDIR /app
        ENV NODE_ENV=production
        # `standalone` carries its own minimal node_modules and server.js; the
        # static assets are deliberately not part of it and have to be placed
        # alongside by hand (a Next.js convention, not an oversight). There is
        # no public/ to copy — this application serves no static files of its
        # own — so adding one means adding a COPY line here.
        COPY --from=build /app/.next/standalone ./
        COPY --from=build /app/.next/static ./.next/static
        EXPOSE 3000
        # Not `npm run start`: there is no npm project here to run a script
        # from, only the server the build emitted.
        CMD ["node", "server.js"]

    # Secrets and the one site this install serves. Never baked into the
    # image, and never read from the site's repository (FR-003d).
    env_file:
      - .env

    environment:
      # The state directory must resolve to the SAME absolute path inside this
      # container and on the host. The application asks the host's daemon to
      # bind-mount a working tree into the agent container, and that daemon
      # resolves the path on the host, not in here — so a named volume, or a
      # different path on either side, produces an agent container mounted on
      # an empty directory and a request that changes nothing. Keep the two
      # halves of the volume line below identical.
      WEBAGENT_STATE_DIR: ${WEBAGENT_STATE_DIR:-/var/lib/webagent}
      AGENT_IMAGE: ${AGENT_IMAGE:-webagent/agent:latest}
      NODE_ENV: production
      # The commit this image was built from, written into the client's .env by
      # ops/release.sh. Served by /api/health so drift across a fleet is
      # checkable from outside the box, and so a rollback is visible as a
      # version going backwards rather than as silence.
      APP_SHA: ${APP_SHA:-unknown}
      # debug | info | warn | error. Structured lines go to stdout, which the
      # daemon's json-file driver captures and the collector tails.
      LOG_LEVEL: ${LOG_LEVEL:-info}
      # Which address the server binds, not a name for this machine. Next's
      # standalone server reads HOSTNAME as its bind address (`server.js`:
      # `process.env.HOSTNAME || '0.0.0.0'`), and Docker sets HOSTNAME to the
      # container id on every container — which resolves, so the server binds
      # that one bridge address and nothing listens on loopback. Published
      # traffic still arrives, so the app looks fine while the healthcheck
      # below, which probes 127.0.0.1 from inside, is refused forever. Binding
      # the wildcard widens nothing: the bridge address was already bound, and
      # the port is published to the host's loopback only.
      HOSTNAME: 0.0.0.0

    volumes:
      # Only the HOST half is configurable, and deliberately so. A client host
      # runs one rootless daemon per Linux user, whose socket lives at
      # /run/user/<uid>/docker.sock; DOCKER_SOCK points at that one. The
      # container half stays the standard path, so dockerode inside the app
      # finds its default and no application code learns which topology it is
      # running under. Unset — a development machine — it is the root daemon,
      # with all the authority the warning at the top of this file describes.
      - ${DOCKER_SOCK:-/var/run/docker.sock}:/var/run/docker.sock
      - ${WEBAGENT_STATE_DIR:-/var/lib/webagent}:${WEBAGENT_STATE_DIR:-/var/lib/webagent}
      # The host admission daemon's socket directory. The directory, not the
      # socket: a bind mount of a missing file makes Docker create a directory
      # with the socket's name, while a missing directory is harmless and
      # lets the socket appear later. Same path both sides, like the state
      # dir, so SLOT_BROKER_SOCKET in .env is right on the host and in here.
      - ${SLOT_BROKER_DIR:-/run/lexi}:/run/lexi

    ports:
      # Loopback only: the app speaks plain HTTP and does no TLS of its own, so
      # a root-owned reverse proxy on the host terminates TLS and forwards to
      # this port. Binding 0.0.0.0 would publish the editing interface on the
      # public interface unencrypted, and Docker writes its own iptables rules,
      # so a host firewall does not reliably save you from that.
      #
      # PORT_HOST, not PORT, and the distinction is not cosmetic. `.env` above
      # is both interpolated by Compose AND passed into the container, where
      # Next reads PORT as the port to listen on — so setting PORT in .env
      # moves the container's listen port and the mapping's host port together
      # and the mapping stops matching. Give each installation on a shared host
      # a distinct PORT_HOST; never put PORT in .env.
      - '127.0.0.1:${PORT_HOST:-3000}:3000'

    # `node -e` rather than curl or wget: node:22-slim ships neither
    # reliably, and node is definitionally present with a global fetch. The
    # liveness route does no I/O, so this cannot fail because GitHub is slow.
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:3000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      # Startup validation reaches GitHub and Netlify before the server
      # answers anything; failing the container while that is still in flight
      # would restart it forever for no reason.
      start_period: 40s

    # A restart brings the process back with no memory of what was running.
    # That is by design: an interrupted request is reconstructed from the pull
    # request it was writing to, and its lock is broken as stale by the next
    # request (FR-009). Nothing is lost that was not ephemeral to begin with.
    restart: unless-stopped

    # Docker's default json-file driver keeps every line forever. Each client
    # runs its own rootless daemon, but all of them write to the one
    # filesystem that also holds /srv/lexi and every client's image store — so
    # without a ceiling, one chatty installation fills the disk and takes down
    # every other client with it. 30 MB per installation is enough to debug a
    # bad release and small enough that twenty of them are a rounding error.
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

    # The application is a Next standalone server that holds no request state;
    # its steady-state footprint is tens of megabytes. The cap is here so that
    # a leak in the app cannot take memory an agent run was budgeted, and so
    # that a host running several installations fails one of them loudly
    # rather than letting the OOM killer choose by size.
    mem_limit: 512m
